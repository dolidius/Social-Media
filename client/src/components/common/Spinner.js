import React from 'react';
import style from '../../CSS-Modules/common/Spinner.module.css';

const Spinner = function() {
  return (
    <div className={style.spinner} />
  )
}

export default Spinner;